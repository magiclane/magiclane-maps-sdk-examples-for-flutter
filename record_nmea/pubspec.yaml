name: record_nmea
description: A Flutter example using Maps SDK for Flutter.

publish_to: 'none' 

version: 1.0.0+1

environment:
  sdk: ^3.8.1

dependencies:
  flutter:
    sdk: flutter

  magiclane_maps_flutter: ^3.1.1

  permission_handler: ^12.0.1
  path_provider: ^2.1.5
  path: ^1.9.1
  file_picker: ^10.2.0
  device_info_plus: ^11.5.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  integration_test:
    sdk: flutter

  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
